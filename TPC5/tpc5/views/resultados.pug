extends layout

block content
  .w3-container.w3-center
    .w3-card-4.w3-center
      header.w3-header.w3-blue-grey
        h1.w3-center 
          b Resultados
      .w3-container.w3-border.w3-responsive
        h4.w3-text-blue-grey
          b Query: 
          b= query
        br 
        if length > 1
          table.w3-center.w3-table-all
            tr
            each h in head
              th.w3-center.w3-text-blue-grey= h
            each k in resultados.results.bindings
              tr
                each h in head  
                  td= k[h].value
        else
          .w3-container
            .w3-row
              .w3-col.m6.l3
                h2.w3-text-blue-grey
                  b Query: 
                  b= query
                |&nbsp;
                |&nbsp;
              .w3-col.m6.m4    
                table.w3-table-all
                  tr
                  each h in head
                    th.w3-center.w3-text-blue-grey= h
                  each k in resultados.results.bindings
                    tr
                      each h in head
                        td= k[h].value
            br
            footer.w3-container.w3-blue-grey
                address Gerado por tpc5APP by tmf